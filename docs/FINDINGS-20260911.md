# 重启后的日志复核与恢复测试

## 已证实的事实

- 2026-09-11 00:59，.99/.100/.101/.102/.104/.105 可登录；.98/.103 连接超时。
- 可登录节点启动时间约 00:22。BeeGFS 挂载因开机时网络未就绪失败；本轮已在 .99/.100/.101/.102 执行 `systemctl start mnt-beegfs.mount`，并确认模型 config.json 可读。
- .99 的旧容器日志表明：2026-09-10 23:57:21 主模型 verify graph 完成，23:57:33 draft graph 完成，随后启动后台健康服务。之前报告称“未通过 graph capture”不完整，不能据此断定 SM121 graph 不兼容。
- .99 于 2026-09-11 00:05 的 scheduler dump 显示包含图像占位 token 的预热请求尚未完成。SGLang `srt/entrypoints/http_server.py::_execute_server_warmup` 在检测到视觉能力时默认发送图像描述请求。
- 同期内核记录 NVIDIA `NV_ERR_NO_MEMORY`、严重内存压力及多个进程等待由 SGLang scheduler 持有的驱动 rw-semaphore。已证实内存压力和驱动阻塞；仍不能将所有现象归因于某一个分配或 kernel。
- 旧运行 KV pool 为 28,719,616 full tokens，计算可用预算约 45 GiB/节点。此规模远大于短文本启动验证的需求。

原始证据保存在 `diagnostics-20260911-010148/`，含六台节点旧容器及事故时间段内核日志。容器时间为 UTC，主机 journal 时间为 UTC+8，以上均转换为北京时间。

## 本轮配置

4 台 .99/.100/.101/.102；主节点数据地址 .187；TP4/EP4/PP1；上下文 8192；max-total-tokens 8192；并发 1；prefill chunk 1024；memory fraction 0.85；关闭 prefill/decode graph；关闭 autotune；跳过自动预热；暂不启用 DSpark；Engram cache 全 TP 合计 8 GiB；模型和代码使用现有 BeeGFS 文件；未 pull 镜像。

首次保守启动使用 0.60，权重加载期间各节点可用内存约 30 GiB，未发生 cgroup OOM。代码复核发现 memory fraction 同时限制权重与 KV 的总预算，0.60 会预留约 46 GiB、可能导致 KV profiling 失败。因此在完成推理前保存日志并改用 0.85，仍以 8192 token 硬上限限制实际 KV 分配；第一次运行不计入吞吐结果。

容器名 `dsv41-spark4-eager-20260911`，API `.99:8104`。cgroup 设置 100 GiB host memory、禁 swap、12 CPU；这不代表对所有 GPU 分配的严格限额。JIT 缓存持久化至各节点 `/data/dsv41-spark-cache`。

启动命令保存于 `20260911-010205/rank*.sh`；进度记录于 `eager4-live/progress.jsonl`；真实请求记录将保存于 `smoke/`。服务启动和正确生成文本分别验证，尚未把端口可访问作为推理成功。

## 权重加载阻塞复核（01:18）

0.85 运行约 10 分钟后仍在 load_weights 的 futures 完成等待。py-spy native 显示多条线程停在 _load_w13/_load_w2 -> copy_ -> cudaMemcpyAsync -> cuMemcpyHtoDAsync_v2 -> pthread_rwlock_wrlock；未见新的内核 NVIDIA OOM。与本地历史文档 docs/AGENTS-提示词-DGX四机vLLM推理集群部署.md:113 记录的网络文件系统 mmap 到 CUDA 加载问题相符，尚不是单凭栈就确定永久死锁。

01:19 保存并停止本轮容器，启用当前代码已有的 SGLANG_MOE_COPY_WEIGHT_VIEWS_BEFORE_H2D=1，先将专家权重视图复制为独立连续 CPU 内存再 H2D。其它参数不变。启动及对照日志目录为 20260911-011857/，含 rank0-native-stack.txt。命令成功启动四个容器。

01:11 前后 .98 与 .103 亦恢复，八台 BeeGFS 挂载均已恢复。

## 顺序复制对照（01:26）

开启权重视图 clone 后，各 rank 在 17:23:25–33 UTC 完成 streaming dequant，随后仍在专家 copy_ 等待。故该开关单独使用尚未解决阻塞，不能宣称有效。

进一步代码检查：模型默认将 CPU 权重复制提交到无界 futures 列表，可能长期保留大量 shard 映射；4 机预分配 GPU 权重后主机可用内存约 28–32 GiB。下一轮仅在容器代码副本中将 deepseek_v4.load_weights 的 use_async_loading 置 False，同时 model-loader-extra-config num_threads=1，保留 clone 开关与 KV 硬上限。配置/对照日志：20260911-012607/。这一变化旨在限制并行权重视图存活，不改变数学计算或模型精度。

本地 /data/models 中的旧 48 分片属于 V4，不能直接代替 V4.1。4 节点本地盘剩余 319–441 GiB；V4.1 非 Engram 分片合计 286.124 GiB，Engram 分片还含少量投影权重，不能整文件略去。

## 双组加载对照（01:31）

顺序加载 A 组 (.99–.102) 的 py-spy 局部变量连续样本从 model.layers.4 推进至 layers.6、layers.11，确认其仍在前进，不能称为永久死锁。

B 组使用空闲 .98/.103/.104/.105，TP4/EP4，主节点 .184:29544，HTTP .98:8104，容器 dsv41-spark4-cloneall-20260911。除已有的顺序复制/1 线程/clone 开关外，在容器私有副本中令 _copy_weight_view_before_h2d 对所有 CPU 权重执行 clone，避免“完整 tensor 且 storage 恰好等大”的早返回仍保留 mmap backing。共享源文件和原始权重未改动。B 组启动目录 20260911-013148/。

.98 的系统时钟比控制端约慢 44 分钟，所有客户端吞吐使用 monotonic perf_counter，不用跨节点 wall clock 相减。实验进度以控制端时间记录；.98 容器日志不得直接与其它节点时间等同比较。

## 成功文本生成与 C1 基线（01:44–01:49）

A 组 rank0 完整 Load weight end 为 778.38 s；B 组为 575.50 s。两组均完成 KV 初始化（full=8192），GPU 权重报告约 73.70 GiB。HTTP ready 后的 /health 触发文本探测及首次 Triton JIT，暂时健康检查超时；之后恢复 200。真实 /generate 输入 The capital city of France is，两组均返回 Paris.（3 completion tokens 含 EOS）。短 smoke 含约 14 s 首次 JIT，不用于稳定 TPS。

随后采用 tonyd2wild 仓库 prompts-v1 的计数、代码、正文三类 C1：A 组 decode 11.73 / 11.60 / 10.98 TPS，B 组 13.99 / 11.76 / 11.43 TPS（客户端流式计时、usage token 计数、结束事件包括 EOS）；两组三类输出逐字一致，计数完整 1–80。代码达到 200-token 上限，未宣称所有代码样本可独立执行。TTFT 约 0.28–0.66 s。两组物理节点不同且共享 BeeGFS，不能把 decode 差异归因于加载补丁。原始 SSE 与汇总见 recovery/bench/。

四 rank 的 Engram native reader 做 536 次原始行/缓存/边界/非拥有行置零校验，0 mismatch：engram-byte-parity-20260911.json。

## 新参考仓库与 GPU 状态（01:52–01:54）

参考 tonyd2wild/DeepSeek-V4.1-Flash-vLLM-DGX-Spark，固定审阅 commit ca662ac35193c69ace9cee37f13a94abf2eff0fc。网页搜索缓存显示 boot8，固定 commit 已是 boot10，引用性能必须以该 commit 及原始 results/boot10 为准。

按仓库 tools/gpuflip.py 在八台已无推理请求、但模型驻留的容器内测试约 80 s：.101/.102 的 GEMV 出现约 55–80 GB/s 的低速片段，另六台约 220–239 GB/s；.101 还有 57 TFLOPS matmul 中位数的慢片段。此为本机实测线索，不足以单独证明历史 8 机性能瓶颈。首次原始记录 gpu-state-20260911-015213/。随后对 .99（正常对照）/.101/.102 复测并附加 NVML 频率/功耗/温度/限频原因采样。

下一轮 B 组仍用 .98/.103/.104/.105：从参考工具借鉴每 rank 本地稀疏 Engram 副本（约 47.21 GiB），保留全局文件偏移，字节抽检通过后才写 verified.json。SGLang 适配器增加源 index SHA256、行范围、完整文件头一致性检查；本地副本只用于 Engram reader，模型 loader 始终使用完整原始 checkpoint。借鉴代码的 MIT 许可证保留于 stage_engram_rows.LICENSE。

复测 gpu-state-20260911-015448/：.101 再次捕获约 70–75 GB/s 与 228 GB/s GEMV 状态；慢/快样本最近 NVML 记录的频率中位数均 2405 MHz、温度中位数均 63°C、active throttle reasons 均 0，功耗中位数约 20.94/30.05 W。.99 保持约 223 GB/s。.102 第二次 GEMV 快，但部分 matmul/copy 段仍变慢。当前证据支持“间歇性能状态波动”，不支持“固定低频”结论；尚未修改时钟/电源/固件设置。


## 优化版启动与正确性扩大验证（02:13–02:25）

4机 B 本地 Engram + DSpark block5 + decode full graphs BS1..8 于控制端约02:13就绪。rank0 主权重601.06s、draft148.41s；target graph24.29s、draft graph9.93s。计数3次60.00/72.72/65.89 TPS，代码48.44/59.41/57.95，正文19.20/27.43/28.02。计数均完整1..80，代码达到200-token限制，正文可读；这些是限定提示词数据，不能证明整体服务正确。

扩展到参考仓库完整任务集时，C1带标签JSON返回无关句子、summary返回无关道歉、format重复无关内容，已主动中止C1–C8矩阵并保留原始SSE。矩阵标记valid_for_performance_comparison=false，不能将其吞吐作为有效成绩。去除标签后54-token JSON正常，但74-token format与292-token summary仍错；flush_cache后仍复现。当前不能认定DSpark或graph就是根因。

独立数值检查：原生Engram两层、6种长度、每种3次新行号graph replay逐位匹配原checkpoint（engram-graph-parity.log）；FP8 packed attention在query32/64/65/74/292、extra页128/64与无extra的测试未出现跨64边界突变（attention-numeric-original.log，relative RMSE约2%–2.5%，包含内核FP8量化误差）。这不能替代实际模型输入验证。

A组已保存日志停止，02:19重新启动原eager配置用于扩大正确性对照（20260911-021927）。B组随后单改SGLANG_SM120_FLASHMLA_BACKEND=triton，保留DSpark/graph/本地Engram做对照。8机本地Engram均已校验完成，但性能测试推迟到正确性问题明确后。

GPU独立探针：.101卸载模型后GEMV中位数约225–229GB/s，两轮均未复现约70GB/s慢态（gpu-state-101-no-model.log）。仍无法仅据此区分内存驻留、驱动状态与负载历史的作用。


## 扩大 eager 正确性对照（02:34–02:38）

原eager无DSpark、无graphs、BeeGFS Engram配置重启后，代码语法、JSON、叙事、正文、数学14:06、逻辑题基本结论、表格8.95与计数均通过自动检查。292-token摘要仍错误地声称输入被损坏，因此不能把全部错误归因于新增优化。74-token表格在eager中正确，DSpark+graph版本不正确，存在路径差异。9种前置背景长度（总输入20–1047tokens）的Paris问题都答对，但该问题仅涉及常识、不能验证长上下文信息保真，且背景重复可命中prefix cache。独立16-head注意力数值对照同样未复现64-token边界突变。

Triton注意力DSpark+graph组约02:38就绪，已开始相同完整正确性套件。以上数据均不替代质量基准评测。


## Triton 对照通过与混合注意力（02:39–02:44）

B组单改MLA backend为Triton，保留DSpark5、decode full graphs、local Engram，完整9类基础检查和9项常识长度扫描均通过。相同292-token摘要正确输出3条要点，相同74-token表格总计8.95。比起原FlashInfer路径，这是实际端到端正确性改进；不能仅凭该对照确定更深层的根因是量化误差还是具体实现bug。

同参考提示C1两次：计数46.50/44.37 TPS，代码35.66/36.55，正文17.30/17.44（024212-spark4-triton-dspark-graph）。功能性套件代码321tokens的29.23TPS与200token截断基准不是同一长度，分开保留。

02:44启动混合方案，恢复FlashInfer <=64-query路径，仅将>64-query路径换为Triton。独立adapter目录/mnt/beegfs/dsv41-spark-adapter-hybrid；--mla-backend hybrid设置DSV41_PREFILL_TRITON=1。launcher保存全部原Triton日志于20260911-024408。可显式创建/tmp/dsv41-prefill-compare文件启用实际输入两种注意力对照，默认关闭，正式计时不得启用。此组合尚待完整验证。


## 03:06 混合注意力验证与矩阵

- 4机混合通过9类基础检查、9个Paris长度扫描、1502/2922/5914-token独立记录码检索。
- C1三次计数70.79/71.04/71.72，代码57.80/57.92/58.43，正文27.37/26.50/27.60 TPS。
- C1–C8三类矩阵全部通过基本内容检查；C8 aggregate计数284.16、代码207.87、正文87.81 tokens/s。
- 原eager无标签摘要反复失败，但带标签JSON/summary/format全部正确。不存在简单的“所有>64token失败”结论。
- eager独立检索全部正确，但预热后prefill仍只有86.8/89.8有效token/s；混合本地约2950token时256.5 token/s。多变量配置差异，不能仅归因Engram。
- 对实际292-query摘要、rank0的40层FlashInfer和Triton prefill逐层对比：relative RMSE约0.00615–0.00958，未出现非有限值或巨幅跳变。这个小差异并未证明异常输出根因，不能宣称已定位某个FlashInfer bug。诊断flag已移除，之后性能无双算。
- 测试链初次/flush_cache因刚结束请求仍在清理返回400，未到性能环节；增加2秒重试后继续，不重复已完成正确性测量。


## 03:16 八机混合路径启动

- 两组4机已于03:07归档并停止；8机启动配置位于20260911-030743，全部节点预检查通过。
- 03:16八机服务就绪，decode日志确认cuda graph=True，prefill图关闭。KV最终full=8192（自动估算的31030016随后被硬上限覆盖，不能误读初始估算为实际分配）。
- 主节点两条NCCL指定网卡实查各100000Mb/s、Link detected=yes；RDMA四端口均ACTIVE。未跑聚合链路吞吐，不把两条100Gb/s直接当作200Gb/s有效通信。


## 03:24 最终4/8机数据与波动复测

- 每组24个矩阵单元/108条请求，18个基础及长度检查、3个随机检索、2个prefill点全部完成，数据完整性检查通过。
- 4/8机C1中位decode：计数71.04/78.32，代码57.92/67.03，正文27.37/28.58。提升约10.3%/15.7%/4.4%，不是线性扩展。
- C8总吞吐4/8：计数284.16/293.84，代码207.87/216.19，正文87.81/84.21。单次批次结果，不能判断小差异统计显著性。
- 2950-token TTFT4/8为11.502/7.062秒；5853-token为23.141/13.182秒。有效prefill4/8为256.5/417.7及252.9/444.0 token/s；非纯GPU kernel指标。
- 8机六轮连续计数（独立NVML诊断）69.64、86.15、85.34、85.99、84.80、85.47 TPS；中位85.40，全部输出相同且1–80正确。各节点时钟中位约2392–2411MHz、限频flags全0。首轮节点功耗中位均值26.49W，其余约30.4–30.8W，不能证明因果或固定低频根因。
- 8台最终运行状态均running、OOMKilled=false，完整日志final-spark8-20260911-032417；服务保留于192.168.32.98:8108。4机组已归档停止。
- 主模型加载8机各rank250.93–337.94秒；draft7.78–72.24秒；target图约14.83–15.02秒、draft9.67–9.70秒。


## 12:42 FlashInfer 0.7 四机优化复测

- 从已有本地 ARM64 镜像提取 FlashInfer 0.7.0rc1，构建派生 SGLang 镜像并经 BeeGFS 分发；全程没有执行 `docker pull`。
- FlashInfer 0.7 通用调度器可能把 decode batch 交给当前 DSV4 page/top-k 组合不支持的 prefill specialization，导致 BS1–8 CUDA graph capture 失败。SM120 adapter 改为 decode batch≤64时直接调用 `sparse_mla_sm120_decode_dsv4`；更大 query batch 在 `DSV41_PREFILL_TRITON=1` 下继续使用已验证 Triton 路径。
- 最终4机配置为TP4/EP4/PP1、DSpark block5、local Engram、decode graphs BS1–8、context/KV8192、chunked prefill1024。功能、长度和独立记录码检索均通过。
- C1三次中位计数77.66、代码58.17、正文16.86 TPS。相对0.6.18基线，计数+9.3%、代码+0.4%；正文的DSpark接受率仅约21%–58%，低于计数约90%–97%，因此不能把正文退化直接归因为attention kernel。
- 明确预热shape后的2950/5853-token有效prefill为347.6/351.2 TPS，相对原4机256.5/252.9提升35.5%/38.9%。未预热的新shape包含Triton JIT，不计为稳态prefill。
- C8 aggregate计数281.51、代码204.97、正文77.25 TPS。将chunk增至8192未改善prefill：FlashInfer通用prefill约86–89 TPS，真实Triton hybrid冷测约66–78 TPS；大query batch不适合当前Triton实现，故最终保留1024。
- 这轮修复缩小了部分差距，但仍未达到同4机vLLM的91.98/75.54/30.79 C1中位。主要剩余项包括跨机EP每步专家通信、world-size 4/8下不可用的FlashInfer all-reduce、共享专家融合差异和DSpark提示词依赖的接受率。
